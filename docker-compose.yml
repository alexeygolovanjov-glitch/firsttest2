services:
  web:
    build: .
    container_name: firsttest-cinema
    restart: unless-stopped
    environment:
      APP_NAME: ${APP_NAME:-FirstTest Cinema}
      ADMIN_TOKEN: ${ADMIN_TOKEN:-change-this-token}
      DATABASE_PATH: ${DATABASE_PATH:-/data/app.db}
    ports:
      - "${PORT:-8000}:8000"
    volumes:
      - ./data:/data
